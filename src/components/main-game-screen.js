import React from 'react';
import { View, Text } from 'react-native';

const MainGameScreen = () => (
  <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
    <Text>Main Game Screen</Text>
  </View>
);

export default MainGameScreen;
